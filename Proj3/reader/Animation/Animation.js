/**
 * Create an animation
 * @constructor
 */
function Animation() {

}

/**
 * This function here is virtual. It computes into the matrix the exact place of the animation
 * @param {Matrix} m - The matrix of the animations
 * @param {Number} time - The to be processed
 */
Animation.prototype.computeMatrix = function(m, time) {
   
}

//Animation.prototype.[nome da funcao]=function([argumentos]){}